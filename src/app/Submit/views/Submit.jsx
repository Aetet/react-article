/**
* @jsx React.DOM
*/
var React = require('react');
var Submit = React.createClass({
  render: function () {
    return (
      <div>
        <button>Submit</button>
      </div>
    );
  }
});
module.exports = Submit;